import 'package:flutter/material.dart';

class AppColors {
  // Main colors
  static const background = Color(0xFF121212);  // Dark background
  static const textPrimary = Color(0xFFFFFFFF);  // White text
  static const textSecondary = Color(0xFFB3B3B3);  // Light gray text
  
  // Accent colors
  static const accent = Color(0xFF00E676);  // Vibrant green accent
  static const accentLight = Color(0xFF69F0AE);  // Light green accent
  
  // Utility colors
  static const divider = Color(0xFF2A2A2A);  // Dark gray divider
  static const error = Color(0xFFFF5252);  // Bright red for errors
  static const success = Color(0xFF69F0AE);  // Light green for success
  
  // Additional colors
  static const cardBackground = Color(0xFF1E1E1E);  // Slightly lighter than background
  static const inputBackground = Color(0xFF2A2A2A);  // Input field background
  static const overlay = Color(0x80000000);  // Semi-transparent black for overlays
} 