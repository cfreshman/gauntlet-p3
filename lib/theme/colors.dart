import 'package:flutter/material.dart';

class MinecraftColors {
  // Base colors
  static const sandstone = Color(0xFFDDD4B5);
  static const lightSandstone = Color(0xFFE6DEC4);
  static const redstone = Color(0xFFB02E26);
  static const darkRedstone = Color(0xFF932520);
  static const textColor = Color(0xFF000000);

  // Prevent instantiation
  MinecraftColors._();
} 