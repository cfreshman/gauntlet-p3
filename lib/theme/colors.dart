import 'package:flutter/material.dart';

class AppColors {
  // Main colors
  static const background = Color(0xFFEEEBE6);  // Light beige background
  static const textPrimary = Color(0xFF222222);  // Dark text
  static const textSecondary = Color(0xFF666666);  // Lighter text for subtitles
  
  // Accent colors
  static const accent = Color(0xFF222222);  // Dark accent
  static const accentLight = Color(0xFFDDDDDD);  // Light accent for buttons/cards
  
  // Utility colors
  static const divider = Color(0xFFDDDDDD);
  static const error = Color(0xFFE53935);
  static const success = Color(0xFF43A047);
} 